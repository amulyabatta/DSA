arr = list(map(int, input().split()))
k = arr[0]        # store the first element
i = 0
for j in range(1, len(arr)):
    arr[i] = arr[j]   # shift elements left by one position
    i += 1
arr[len(arr)-1] = k   # put the first element at the end
print(arr)
